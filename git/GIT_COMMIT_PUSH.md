1 — Close all open files to make sure they are saved:

2 — Run:

```bash
git status
git add .
git commit -m "[...]"
git push
```

2.1 — If git push returns an error:

```bash
git push --set-upstream origin [your branch]
```
